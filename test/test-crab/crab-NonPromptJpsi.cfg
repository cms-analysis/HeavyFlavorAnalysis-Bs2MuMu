[CRAB]

jobtype                 = cmssw
scheduler               = glitecoll

[CMSSW]

datasetpath             = /NonPrompt_Jpsi_mumu/CMSSW_1_3_1-Spring07-1547/GEN-SIM-DIGI-RECO
pset                    = test-crab.cfg
total_number_of_events  = -1
events_per_job          = 10000
output_file             = output.root

[USER]
return_data             = 0
use_central_bossDB      = 0
use_boss_rt             = 0
ui_working_dir          = /home/ceggel/cern_cvs/cmssw/CMSSW_1_3_1/src/HeavyFlavorAnalysis/Bs2MuMu/test/NonPrompt_Jpsi_mumu

### COPY JOB OUTPUT INTO A SE ###
## if you want to copy the CMS executable output into a SE (i:e castor)
### WARNING: if the copy fails and return_data = 0, the output is lost
copy_data = 1

### if copy_data = 1 ###
## name of the SE where to copy the CMS executable output.
storage_element = storage01.lcg.cscs.ch
## and the SE directory (or the mountpoint) that has to be writable from all
storage_path = /pnfs/lcg.cscs.ch/cms/local/eggel/analysed_Spring07_noHLT_new/NonPromptJpsi

[EDG]
rb                      = CERN 
proxy_server            = myproxy.cern.ch 
virtual_organization    = cms
retry_count             = 5
lcg_catalog_type        = lfc
lfc_host                = lfc-cms-test.cern.ch
lfc_home                = /grid/cms
CE_white_list           = cscs
